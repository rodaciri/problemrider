# For similar writing of titles
titlecase

# For working with Jekyll markdown files
PyYAML

# For embeddings-based related_problems generation
sentence-transformers>=2.2.0  # Best quality embeddings (~90MB download)
scikit-learn>=1.2.0           # Fallback TF-IDF approach
numpy>=1.21.0                 # Required for embeddings

# For robust markdown text extraction
markdown>=3.4.0               # Convert markdown to HTML
beautifulsoup4>=4.11.0        # Extract plain text from HTML

# For local embedding service support
requests>=2.28.0              # HTTP client for local embedding API
tqdm>=4.64.0                  # Progress bars with timing information

# For causal relationship validation
networkx>=2.8.0               # Graph structure for causal modeling
openai>=1.0.0                 # LLM-based causal reasoning
